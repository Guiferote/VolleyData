@page
@model VolleyData.Pages.Tecnicos.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Tecnico</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Tecnico.Id" />
            <div class="form-group">
                <label asp-for="Tecnico.Nome" class="control-label"></label>
                <input asp-for="Tecnico.Nome" class="form-control" />
                <span asp-validation-for="Tecnico.Nome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tecnico.EquipeId" class="control-label"></label>
                <select asp-for="Tecnico.EquipeId" class="form-control" asp-items="ViewBag.EquipeId"></select>
                <span asp-validation-for="Tecnico.EquipeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
